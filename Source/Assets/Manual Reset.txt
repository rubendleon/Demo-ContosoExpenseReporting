1. In the Windows Azure Portal, delete all VMs except for MyCoolMV

2. Remove the storage account (unless it was created with name contosoexpense, in which case the script will remove it)

3. Remove all the storage accounts named "contosoreceipt#" (where # could be any number)

4. Login to the windows azure portal (the browser will be already open). Use this credentials:

Primary:
	- dpedemo13@live.com
	- AzureRocks

Shadow:
	- dpedemo5@live-int.com
	- AzureRocks

Backup:
	- dpedemo14@live.com
	- AzureRocks
